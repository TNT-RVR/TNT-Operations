# Mock-data dev mode: `npm run dev:mock`.
#
# The committed `.env` points at the live Supabase project, which means the
# ordinary dev server sits behind a real login. This mode runs the seeded
# MockProvider instead — no backend, no credentials — for working on UI.
VITE_DATA_SOURCE=mock
