# dependencies
node_modules

# build output
dist
dist-ssr

# local env — NEVER commit secrets (Govee key, Maps key, Supabase keys, SMTP)
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# generated data import (may contain real operational data — never commit)
scripts/incubation_import.sql
scripts/import_parts/
scripts/fields_import.sql
scripts/field_analysis_import.sql
